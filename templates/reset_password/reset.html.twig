{% extends 'layouts/base.html.twig' %}

{% block title %}Panterest | Reset your password{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="mt-5 mb-5 text-center">Redéfinir votre mot de passe</h1>

            {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <button type="submit" class="btn btn-primary btn-lg btn-block">Appliquer le nouveau mot de passe</button>
            {{ form_end(resetForm) }}
        </div>
    </div>
{% endblock %}
